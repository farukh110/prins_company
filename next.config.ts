// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: 'export',
//   trailingSlash: true, // Ensures clean URLs (e.g., /prins_company/ instead of /prins_company)
//   images: { unoptimized: true }, // Required for static export to handle images
//   basePath: '/prins_company', // If deploying to a subfolder; matches your URL
// };
// module.exports = nextConfig;

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

// export default nextConfig;

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   output: 'export',
//   basePath: '/prins_company',   
//   images: {
//     unoptimized: true,
//   },
// };

// module.exports = nextConfig;
